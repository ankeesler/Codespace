Initial commit of Codespace!

This commit is of version 1.0.

Features include:
	* ./project creates a Makefile and a main.c file
	* ./new creates new files in different languages and adds them to the Makefile	
